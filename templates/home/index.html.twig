{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}


	<h1>Chats</h1>
	<ul>
		<img src="{{ cat.picture }}" width="300" height="200" alt="{{ cat.name }}" id="cat-picture">
		<li id="cat-name">{{ cat.name }}</li>
		<li id="cat-race">{{ cat.race }}</li>
		<li id="cat-age">{{ cat.age }}</li>
		<li id="cat-localisation">{{ cat.localisation }}</li>
		<img src="{{ asset('images/actionbuttons/dislike.png') }}" data-cat-id="{{ cat.id }}" class="dislike-button" alt="Dislike" width="50" height="50">
		<img src="{{ asset('images/actionbuttons/like.png') }}" data-cat-id="{{ cat.id }}" class="like-button" alt="Like" width="50" height="50">

	</ul>

	<div id="flash-message" class="hidden">
		<p id="flash-content"></p>
	</div>
{% endblock %}
